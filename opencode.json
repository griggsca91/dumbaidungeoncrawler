{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "brave": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@brave/brave-search-mcp-server",
        "--transport",
        "http"
      ]
    }
  }
}